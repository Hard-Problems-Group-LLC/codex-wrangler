Completed Tasks

Record completed tasks with newest entries at the top. Use asterisk-plus-blank
line formatting. Include concise outcomes, owners, blocker resolution notes,
and ISO 8601 timestamps.

*
  Completed: 2026-09-02T02:19:09-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: preserved the affected project-local Codex context and both involved
  working trees in verified owner-only compressed archives; established that
  all 84 rollout transcripts and six SQLite databases were recoverable; traced
  the segmentation fault to two exact 8 MiB native-file prefixes plus a
  truncated cached package; reinstalled the authenticated exact Codex version;
  and resumed the selected live thread without changing any rollout bytes.
  Added portable native-payload extent validation, a launch-time health gate,
  and a standalone `--repair /absolute/project/root` operation that proves
  ownership and exact-version evidence before bounded cleanup while preserving
  context and history. The health gate accepts one complete version line among
  surrounding diagnostics while rejecting inline and duplicate claims.
  Validation: all archives passed gzip and SHA-256 checks; database integrity,
  quick, and foreign-key checks passed; the repaired target passed exact native
  digest checks, inspection, self-test, npm audit, launcher version/help, and a
  controlled live resume; the repository quality gate passed with 221 tests.

*
  Completed: 2026-07-10T04:12:04-07:00.
  Owner: Codex and operator.
  Requestor: operator.
  Outcome: recovered the Rocky Linux 10.2 transplant with a directly
  executable bootstrap wrapper, user-owned pinned pyenv reuse, Python 3.14.6
  runtime selection, standard-venv base-drift rebuilding, and venv-safe nested
  validation. Preserved the 154,483,126-byte project-local Codex home and its
  memories, sessions, rules, goals, history, and authentication while repairing
  the managed stable Codex 0.144.1 package tree. Added a specification, two
  upstream ECRs, and repository recovery notes, then removed the emergency root
  npm install after the managed launcher passed.
  Validation: the full Black, Ruff, compileall, entropy, tripwire, and pytest
  gate passed with 164 tests on Python 3.14.6. Real standard and development
  installs passed; inspect had zero issues and warnings; self-test and npm audit
  passed; isolated launcher version and transplanted ChatGPT login status
  passed; post-removal inspect remained clean.

*
  Completed: 2026-06-02T12:47:05-07:00. Extended repair install cleanup after
  inspecting the operator's timed-out npm install. Outcome: confirmed
  the project-local npm temp tarball was truncated, added removal of
  `.codex-local/.npm-cache/_cacache/tmp` to `--repair-install`, and
  updated README, specification, changelog, and tests. Validation:
  `.venv/bin/python scripts/run_local_quality_gate.py` passed, including
  Ruff, compileall, entropy checks, tripwire verification, and 155 pytest
  tests.

*
  Completed: 2026-06-02T12:22:10-07:00. Added managed npm install progress logging
  after the operator noted that the bounded install command could still
  sit silently. Outcome: kept npm spinner progress disabled, added default
  HTTP fetch logging and foreground lifecycle-script output, exposed
  `--npm-install-loglevel` for quieter or noisier installs, and updated
  tests plus docs. Validation: `.venv/bin/python
  scripts/run_local_quality_gate.py` passed, including Ruff, compileall,
  entropy checks, tripwire verification, and 155 pytest tests.

*
  Completed: 2026-06-02T12:10:22-07:00. Fixed managed Codex npm install hangs reported
  by the operator. Outcome: added bounded managed npm subprocesses, a
  `--npm-timeout-seconds` override, install-only npm audit/funding/
  update-notifier/spinner suppression, default HTTP fetch and foreground
  script logging, README and changelog notes, a bounded npm operations
  specification, and regression tests. Validation:
  `.venv/bin/python scripts/run_local_quality_gate.py` passed, including
  Ruff, compileall, entropy checks, tripwire verification, and 155 pytest
  tests.

*
  Completed: 2026-05-03T00:07:59-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: updated the `TheKnowledge` submodule from `dcf9e09` to `76202ee`
  through the managed helper, refreshed the managed AGENTS footer guidance,
  and revalidated the checkout on the updated baseline while preserving the
  repo's existing bootstrap contract. The managed refresh briefly proposed
  replacing repo-specific bootstrap behavior, but that diff was kept out of
  the final tree because the current committed implementation remains the
  intended local contract.
  Validation: `.venv/bin/python -m pytest tests/test_dev_setup.py
  tests/test_bootstrap.py tests/test_entrypoints.py` passed, and
  `.venv/bin/python scripts/run_local_quality_gate.py` passed with 150 tests
  green after the submodule update.

*
  Completed: 2026-05-02T22:58:30-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: repaired the managed developer bootstrap so a freshly installed
  explicit Python 3.12 runtime can build the repo-local `.venv` before Black
  exists, made the TheKnowledge hook-installer fallback target the parent
  repository instead of the submodule gitdir stub, bounded automatic `direnv`
  download failure with an explicit timeout, and kept the managed `.gitignore`
  generation in sync with the requested `.local/` ignore entry. Then
  completed the supported `./install.sh --mode dev --user-home /home/mheck`
  flow, which now provisions `/home/mheck/.pyenv`, `.python-version`
  selecting `3.12.12`, the repo-local `.venv`, managed git hooks,
  `/home/mheck/.local/bin/codex-wrangler`, `/home/mheck/.local/bin/direnv`,
  and the repository `.envrc`.
  Validation: `.venv/bin/python -m pytest tests/test_dev_setup.py
  tests/test_bootstrap.py tests/test_entrypoints.py` passed before and after
  the bootstrap fixes, `.venv/bin/python scripts/run_local_quality_gate.py`
  passed with 150 tests green, and the resulting managed environment reports
  `Python 3.12.12`, `black 26.3.1`, `pytest 9.0.2`, and `direnv 2.37.1`.

*
  Completed: 2026-04-30T18:41:41-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: drafted and exercised a TheKnowledge ECR plus executable companion
  proposed `install-stage-2.py` candidate that make the managed starter
  refresh path preserve explicit user-home targeting, reject implicit
  installs into project-local AI homes, pass resolved launcher paths to
  project hooks, and document overlay-preservation requirements for managed
  files.
  Validation: `git diff --check`, `.venv/bin/python -m py_compile`,
  `.venv/bin/python scripts/run_black_safe.py --check`, high-confidence
  credential grep, and direct parent-repo entropy scanning of the new ECR
  files passed. The candidate also passed temporary managed-layout smokes and
  the existing stage-two entrypoint tests when temporarily installed at
  `scripts/install-stage-2.py`. The local quality-gate entropy and tripwire
  commands also passed for their default configured scope. A separate
  parent-root entropy coverage bug was recorded after broad scans traversed
  ignored local Codex artifacts unless explicit excludes were supplied.

*
  Completed: 2026-04-30T18:16:10-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: updated the TheKnowledge submodule from `9c75ca9` to `dcf9e09`
  and refreshed the managed AGENTS footer guidance for Markdown proposal
  records, maintained prose formats, and ACP scope. The managed submodule
  update helper also attempted to refresh `scripts/install-stage-2.py`, but
  that generated change was backed out because it removed `--user-home`
  support and failed the existing entrypoint contract test.
  Validation: high-confidence credential scans found no matches.
  `git diff --check`, `.venv/bin/python scripts/run_black_safe.py --check`,
  and `.venv/bin/python scripts/run_local_quality_gate.py ruff compileall
  entropy_check entropy_tripwire_verify pytest` passed with 145 tests green.

*
  Completed: 2026-04-30T17:33:10-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: hardened project-local OpenAI Codex install management after npm
  package behavior changes broke provisioning and upgrade attempts. Replaced
  generated launcher use of `npx codex` with direct execution of the managed
  `.codex-local/node_modules/.bin/codex` binary, routed managed npm operations
  to `.codex-local/.npm-cache`, added `--repair-install` for canonical managed
  npm artifacts, added post-install Codex binary smoke checks, and expanded
  inspection and self-test reporting for corrupt metadata, package manifests,
  lockfiles, missing platform packages, missing `node_modules`, invalid audit
  JSON, and unsafe managed tree-removal shapes. Documented the repair boundary
  and operator-owned backup cleanup expectations in `README.md` and
  `CHANGELOG.md`.
  Validation: `git diff --check`, `.venv/bin/python
  scripts/run_black_safe.py --check`, and `.venv/bin/python
  scripts/run_local_quality_gate.py ruff compileall entropy_check
  entropy_tripwire_verify pytest` passed with 145 tests green. A live
  broken-state smoke with corrupt `.codex-local/package-lock.json` and missing
  `.codex-local/node_modules` now reports classified inspect issues instead
  of raising a traceback.
  Notes: the user-local installed `codex-wrangler` command still needs
  `./install.sh` rerun after this source patch. The intentional break-test
  backup `.codex-local/node_modules.break-test` remains for operator cleanup
  after repair.

*
  Completed: 2026-04-19T14:57:30-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: fixed the CLI error path for mistyped long-form flags such as
  `codex-wrangler update`. Bare words remain positional project-root
  arguments, so the command semantics did not change. When no such directory
  exists and the token matches a known long-form flag without `--`, the CLI now
  reports a friendly error with a hint such as
  `Did you perhaps mean '--update'?`; existing directories named `update`
  still work as project roots. Added a specification, regression tests,
  changelog entry, and closed bug record.
  Validation: `git diff --check` passed. Black, Ruff, compileall,
  entropy_check, entropy_tripwire_verify, and full pytest passed through
  `scripts/run_tool_with_timeout.py`; full pytest reported `133` tests green.
  Focused `tests/test_config.py` and `tests/test_entrypoints.py` passed with
  `32` tests green. Manual smokes confirmed `codex-wrangler update` and
  `codex-wrangler force` report missing-dash hints and an existing directory
  named `update` is still used as a project root.

*
  Completed: 2026-04-15T21:04:00-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: fixed the reasonable-permissions launcher default so generated
  `bin/codex-local` wrappers use `-a on-request -s workspace-write` instead
  of `-a never -s workspace-write`. This keeps normal workspace edits flowing
  while allowing Codex to request escalation for ACP workflows that need
  `.git` writes or network push access. Updated the specification, approved
  proposal amendment, generated README text, regression tests, changelog, and
  bug lifecycle records. Documented the preferred boundary where local
  add/commit can be approved as maintenance work while push remains
  explicit publication approval.
  Validation: Black, Ruff, compileall, entropy_check,
  entropy_tripwire_verify, and full pytest passed through
  `scripts/run_tool_with_timeout.py`; full pytest reported `127` tests
  green. Focused `tests/test_rendering.py`, `tests/test_config.py`, and
  `tests/test_operations.py` passed with `39` tests green.

*
  Completed: 2026-04-15T20:35:00-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: added a codex-wrangler local policy that prefers patch-style edits
  for small localized changes while permitting explicit non-destructive
  named-file edits or short scripts for whole-file records, generated content,
  mechanical updates, or patch-helper failures, including a concrete local
  reliability record for the current `apply_patch` sandbox failure. The
  policy also requires
  generally unreliable tools to be recorded and surfaced to the operator or
  demoted in the repo-local strategy set so agents do not keep
  rediscovering the same failure. Filed a TheKnowledge ECR under
  `ECRs/TheKnowledge/` requesting the same safety-oriented edit-method policy
  upstream.
  Validation: documentation-only policy change; `git diff --check` was run.

*
  Completed: 2026-04-15T20:26:00-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: added a low-priority open bug record for the intentionally meta
  condition of having no new bugs to add.
  Validation: documentation-only project-management change; no automated tests
  were run.

*
  Completed: 2026-04-15T15:14:36-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: implemented the approved reasonable-permissions launcher-state
  proposal. Added
  `docs/specifications/reasonable_permissions_for_codex_local_launchers.txt`,
  `--set-reasonable-permissions`, `--clear-reasonable-permissions`, persisted
  `reasonable_permissions_enabled` metadata, inspect output, generated local
  README visibility, and launcher logic that adds
  `-a never -s workspace-write` only when callers did not already choose
  approval or sandbox behavior. Toggle-only set and clear calls against an
  existing managed install now preserve the current package selection and
  rewrite managed files without invoking npm package resolution or install.
  Added automatic managed `.gitignore` enforcement for `.codex` and
  `bin/codex-local`, refreshed README usage docs, and removed
  `bin/codex-local` from local Git history so the generated launcher no
  longer remains tracked.
  Validation: `bash -n bin/codex-local` passed. `PYTHONPATH=src
  .venv/bin/python -m pytest -q tests/test_config.py tests/test_rendering.py
  tests/test_operations.py` passed with `38` tests green before and after
  Black. Black passed through `scripts/run_tool_with_timeout.py black` after
  setting `THEKNOWLEDGE_BLACK_PYTHON` to `.venv/bin/python` for the wrapper
  child-process runtime. Ruff, `compileall`, `entropy_check`,
  `entropy_tripwire_verify`, and full `pytest` passed through
  `scripts/run_tool_with_timeout.py` with `THEKNOWLEDGE_PYTHON_TOOLS` pointed
  at `.venv/bin/python`; full pytest reported `123` tests green. A dry-run
  smoke check of `--set-reasonable-permissions` confirmed the existing-install
  reconfiguration path skipped npm package operations.

*
  Completed: 2026-04-15T14:48:15-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: translated the NiceBeach reasonable-permissions ECR into a native
  approved `codex-wrangler` proposal under
  `project-management/proposals/approved/`. The proposal keeps the feature
  narrowly scoped to one managed boolean state, launcher default injection of
  `-a never -s workspace-write` only when callers did not already choose
  approval or sandbox behavior, inspect and README visibility, and clean
  regeneration when the state is cleared. It also records a critical
  implementation guardrail: toggling the state must behave as a
  reconfiguration pass that preserves the current package selection instead of
  silently turning into a latest-stable reinstall.
  Validation: manual review confirmed the proposal matches the NiceBeach ECR,
  the current `codex-wrangler` metadata and launcher architecture, and the
  current install-path behavior that would otherwise risk package drift on a
  bare toggle command.

*
  Completed: 2026-04-15T14:17:07-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: split Codex version-catalog refresh from package upgrades.
  `codex-wrangler --update` now refreshes locally known stable, beta, and
  alpha versions without changing the installed package; `--upgrade` now
  requires `--channel`; `--version` now accepts exact versions or
  case-insensitive `latest`; managed metadata now records channel and known
  version tables; and the generated launcher now emits a local update notice
  when the installed Codex version is behind the locally known version on its
  channel. Added fallback state inference for older managed installs so
  package files remain usable when the newer metadata fields are missing.
  Added repo-local cached quality-gate helper wrappers so the standard commit
  helper can run from this repository root. Updated README text, generated
  local README content, and tests to match the new contract. Dog-fooded the
  real user-local install plus a repo-local stable-channel upgrade and
  confirmed `codex-cli 0.121.0` with matching metadata and no inspect
  warnings.
  Validation: `/home/mheck/.pyenv/versions/3.12.11/envs/autotomato-runtime-3.12/bin/python
  -m compileall src tests` passed. `PYTHONPATH=src
  /home/mheck/.pyenv/versions/3.12.11/envs/autotomato-runtime-3.12/bin/python
  -m pytest -q tests/test_config.py tests/test_releases.py
  tests/test_rendering.py tests/test_operations.py tests/test_entrypoints.py`
  passed with `44` tests green. `PYTHONPATH=src
  /home/mheck/.pyenv/versions/3.12.11/envs/autotomato-runtime-3.12/bin/python
  -m pytest -q` passed with `110` tests green. `/home/mheck/.pyenv/versions/3.12.11/envs/autotomato-runtime-3.12/bin/python
  -m ruff check src/codex_wrangler/config.py src/codex_wrangler/layout.py
  src/codex_wrangler/releases.py src/codex_wrangler/rendering.py
  tests/test_config.py tests/test_releases.py` passed. `codex-wrangler
  --inspect .` reported no warnings or issues after the stable-channel
  upgrade, `./bin/codex-local --version` returned `codex-cli 0.121.0`, and
  `python scripts/run_quality_gate_cached.py --show-cache` succeeded from the
  repository root.

*
  Completed: 2026-04-15T12:33:07-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: added a top-level `CHANGELOG.md` in Keep a Changelog style,
  introduced `src/codex_wrangler/changelog.py` plus
  `scripts/git_commit_with_changelog.py` so this repository can preserve
  notable pending-commit summaries in the changelog before the standard
  TheKnowledge commit helper clears the queue, and updated AGENTS, README,
  development docs, git-flow, and state-file guidance to make that workflow
  the local standard. The repo now explicitly treats
  `project-management/completed-tasks.txt` as operational history rather than
  product change history. Drafted the upstream SOP request in
  `ECRs/TheKnowledge/changelog-sop-and-pending-queue-preservation.md`.
  Validation: `python3 -m compileall src tests scripts` passed. `git diff
  --check` passed. `python3 scripts/git_commit_with_changelog.py --help`
  passed. `python3 scripts/git_commit_with_changelog.py --dry-run
  --assume-reviewed -m "Smoke test changelog sync"` passed and confirmed the
  wrapper recognized already-recorded queue entries instead of duplicating
  them. `pytest` remained unavailable in the current interpreters, so the
  full test suite could not be executed here.

* 
  Completed: 2026-04-14T17:13:43-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: hardened the installer trust boundary around isolated AI homes.
  Added `src/codex_wrangler/install_scope.py`, a compact specification, and
  an approved proposal for explicit user-home targeting. Updated
  `scripts/install-stage-2.py` and `src/codex_wrangler/installer.py` so
  user-scoped pyenv, launcher, direnv, and shell-init effects now refuse to
  target a repo-local `.codex-home` silently; operators must provide
  `--user-home PATH` or `--allow-isolated-home` in that situation. Refreshed
  README and development docs, and added tests for explicit user-home
  resolution plus isolated-home rejection.
  Validation: `python3 -m compileall src tests scripts` passed. `git diff
  --check` passed. `python3 scripts/install-stage-2.py --help` and
  `python3 scripts/install_user_tool.py --help` passed and showed the new
  flags. `python3 scripts/install-stage-2.py --force-direct-run
  --skip-submodule-init` and `python3 scripts/install_user_tool.py
  --repo-root .` both failed fast in the current isolated session with the
  expected explicit-targeting guidance. `pytest` remained unavailable in the
  current interpreters, so the full test suite could not be executed here.

*
  Completed: 2026-04-14T14:40:27-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: implemented the approved runtime-fidelity recommendations. Added a
  compact specification plus an approved proposal record, introduced
  `src/codex_wrangler/environment.py` for runtime-signal detection and
  resolved-command reporting, extended inspect and self-test output with
  resolved `node`/`npm`/`npx`, selector, package-manager, and `HOME`/`XDG_*`
  diagnostics, and updated the generated launcher to run a cheap
  selector-aware preflight with `CODEX_LOCAL_PREFLIGHT=warn|strict|off`.
  Updated tests and user-facing docs to cover the new behavior, and refreshed
  the repo's own generated `bin/codex-local` launcher.
  Validation: `python3 -m compileall src tests` passed. `bash -n
  bin/codex-local` passed. `python3 ./codex-wrangler.py --inspect .` passed
  with the new runtime diagnostics visible. `./bin/codex-local --version`
  passed. A throwaway temp-tree smoke check showed warn mode continuing and
  strict mode failing on an explicit `.node-version` mismatch. `pytest` was
  not available in the current interpreters, so the test suite could not be
  executed here.

*
  Completed: 2026-04-14T14:20:29-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: revised the under-review runtime-fidelity proposal into a concrete
  recommendation-led review draft. The proposal now explicitly assesses how
  the intended resulting-project model aligns with modern development and
  security practices, identifies the determinism-versus-isolation tension, and
  recommends a phased change sequence around diagnostics, selector-aware
  preflight, `.codex-home` observability, and parity smoke checks.
  Validation: manual review confirmed the proposal now captures the requested
  policy direction and concrete change set for operator review.

*
  Completed: 2026-04-14T14:14:35-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: documented the current `codex-wrangler` runtime boundary in the
  README, including the fact that `bin/codex-local` localizes the Codex npm
  package tree and Codex state but still resolves `node`, `npm`, and `npx`
  from the active shell `PATH`. Added an under-review proposal that records
  the desired resulting-project model, the human-versus-agent sandbox parity
  concern, and recommended phased future work around runtime diagnostics and
  alignment policy.
  Validation: `git diff --check` passed. `python3
  scripts/run_local_quality_gate.py` failed immediately because
  `/usr/bin/python3` in the current environment does not have the `black`
  package installed. Manual review confirmed the new README section and the
  proposal record match the operator discussion.

*
  Completed: 2026-04-01T10:20:15-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: corrected the repo-specific standard-install hook so a managed
  virtual environment that exposes `bin/python` as a symlink no longer gets
  collapsed to the base interpreter path during project installation. The
  installer now preserves the venv Python path, keeps venv-root detection on
  the managed environment rather than `/usr/bin/python3.13`, and adds a
  focused regression test for the symlinked interpreter case that triggered
  the Debian PEP 668 failure.
  Validation: `.venv/bin/python -m pytest -q tests/test_project_install.py`
  passed with `9` tests green; `.venv/bin/python scripts/run_local_quality_gate.py`
  passed with `71` tests green.
  Notes: a throwaway venv smoke bootstrap could not be completed inside the
  sandbox because the temporary venv could not download `setuptools>=69`
  without network access.

*
  Completed: 2026-04-01T01:42:16-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: corrected the managed install-policy contract so `./install.sh`
  now defaults to a user-local standard install, `./install.sh --mode dev`
  is the explicit repo-local development path, `sudo ./install.sh --system`
  is the explicit system path, `sudo` without `--system` is rejected, and
  direct `root` use without `--system` now requires confirmation. Updated the
  repo's managed install entrypoints, project install hook, and developer
  docs to match.
  Validation: `.venv/bin/python scripts/run_local_quality_gate.py` passed
  with `69` tests green; focused installer and entrypoint regression suites
  passed after the policy correction.
  Notes: stable installs and project-bound development installs now follow
  the same scope rules documented in TheKnowledge.

*

  Completed: 2026-03-31T20:48:26-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: documented the interpreter and package-environment precedence
  follow-up as an approved proposal, added a repo-specific
  `scripts/install_project.py` hook plus `src/codex_wrangler/project_install.py`,
  made development installs publish a project-bound
  `~/.local/bin/codex-wrangler` launcher that points at this checkout's
  `.venv`, made standard and venv-only installs remove that override when
  owned, and taught the stable user installer to reuse the managed pyenv
  runtime when it already exists.
  Validation: `.venv/bin/python -m pytest` passed with `63` tests green;
  `.venv/bin/python scripts/run_local_quality_gate.py` passed; targeted Black
  coverage ran on the new install-hook files; TheKnowledge doc tests and
  entropy checks were rerun after the related standard/proposal updates.
  Notes: the Python development workflow now comes closer to the documented
  standard by keeping the project-specific override active even when the
  command is launched outside the repository directory.

*

  Completed: 2026-03-31T20:08:27-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: adopted the converged managed bootstrap design from TheKnowledge by
  refreshing the repo to the managed `install.sh` plus
  `scripts/install-stage-2.py` contract, aligning docs and tests with the
  shared dev/standard/venv-only install modes, removing the redundant local
  package bootstrap shim, keeping the CLI-specific
  `scripts/install_user_tool.py` path, and synchronizing the `dev` extra with
  the managed pinned tool requirements.
  Validation: `.venv/bin/python -m pytest` passed with `53` tests green;
  `.venv/bin/python scripts/run_local_quality_gate.py` passed after the Black
  26 upgrade; explicit Black coverage also ran on the new untracked Python
  files introduced by the managed refresh.
  Notes: the final parent-repo state now treats `bootstrap.sh` and
  `bootstrap-stage2.py` as compatibility wrappers around the managed install
  flow instead of primary repo-specific bootstrap logic.

*

  Completed: 2026-03-31T15:12:34-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: replaced the fragmented developer bootstrap guidance with a
  top-level `install.sh` plus `scripts/install-stage-2.py`, added Python
  3.9-safe bootstrap helpers and tests, made developer installs rely on
  repo-local `.venv` with mandatory `direnv`, and corrected supporting docs to
  match the new Rocky-and-Ubuntu workflow.
  Validation: `.venv/bin/python -m pytest` passed with 50 tests green;
  `.venv/bin/python scripts/run_local_quality_gate.py ruff` passed; the full
  repo quality gate was rerun after the bootstrap changes.
  Notes: the developer flow now favors user-global `pyenv`, repo-local `.venv`,
  and generated `.envrc` activation instead of editable installs into a
  separate tool venv.

*

  Completed: 2026-03-25T21:26:29-07:00.
  Owner: Codex.
  Requestor: operator.
  Outcome: restructured `codex-wrangler` into focused modules, unified package
  versioning, expanded pessimistic tests, corrected local install and generated
  operator guidance, and hardened local validation around Black sandbox hangs
  with repo-local wrapper scripts.
  Validation: `.venv/bin/python scripts/run_local_quality_gate.py` passed with
  33 tests green plus Ruff, compileall, entropy harness, and entropy tripwire.
  Notes: the Black wrapper now probes optional flag support with bounded
  timeouts, falls back cleanly when probes fail, and formats one file at a
  time so sandbox multi-file hangs do not block the repository quality gate.
