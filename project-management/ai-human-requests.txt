AI-Human Requests
=================

Use this queue for non-blocking actions AI agents need humans to perform.
Record each request with asterisk-plus-blank-line formatting, concise context,
owner/requestor, and ISO 8601 timestamps.

------------------------------------------------------------------------------

Pending Requests
----------------

Active Requests
---------------

*
  2026-07-10T04:06:04-07:00
  Requestor: Codex
  Action: Review and remove the legacy unconditional pyenv initialization block
  from `/home/mheck/.bash_profile` after confirming the managed guarded block
  in `/home/mheck/.bashrc` is the intended sole initialization path.
  Status: active. The duplicate is outside this project's managed file scope
  and currently repeats pyenv/pyenv-virtualenv shim entries in interactive
  shell PATH values.

Completed Requests
------------------

*
  2026-04-15T15:14:36-07:00
  Requestor: operator
  Action: Implement the approved reasonable-permissions launcher-state
  proposal.
  Status: completed by Codex on 2026-04-15T15:14:36-07:00. Added the
  specification, managed config and metadata state, set/clear CLI flags,
  reconfiguration-only toggle behavior for existing installs, launcher
  injection of `-a never -s workspace-write` with explicit caller override
  detection, inspect and generated README visibility, tracked launcher
  refresh, README updates, and focused tests for persistence, clear-path
  regeneration, and launcher override cases.

*
  2026-04-15T14:48:15-07:00
  Requestor: operator
  Action: Orient to `codex-wrangler` and TheKnowledge, then translate the
  NiceBeach ECR for reasonable-permissions defaults into a pre-approved local
  proposal.
  Status: completed by Codex on 2026-04-15T14:48:15-07:00. Added an approved
  proposal under `project-management/proposals/approved/` that scopes the
  change to managed metadata, launcher defaulting, inspect and README
  visibility, and clean clear-path regeneration. The proposal also makes
  explicit that `--set-reasonable-permissions` and
  `--clear-reasonable-permissions` must behave as reconfiguration toggles
  rather than silently reinstalling or upgrading Codex.

*
  2026-04-15T14:17:07-07:00
  Requestor: operator
  Action: Split Codex version-catalog refresh from package upgrades, require
  explicit upgrade channels, add `--version latest|x.y.z`, surface local
  update availability from launcher metadata, and dog-food the result through
  a real user-local install plus a repo-local stable-channel upgrade.
  Status: completed by Codex on 2026-04-15T14:17:07-07:00. The repository
  now treats `--update` as a metadata refresh only, requires `--channel` for
  `--upgrade`, accepts case-insensitive `latest` or exact versions through
  `--version`, records stable/beta/alpha availability in managed metadata,
  warns from `bin/codex-local` when a newer locally known channel version is
  available, and falls back to older managed package files when newer metadata
  fields are absent. Added repo-local cached quality-gate helper wrappers so
  the standard commit helper can run from this repository root. README,
  generated local README content, and tests were updated. Real-install
  dogfooding in this repo confirmed a clean stable upgrade to `codex-cli
  0.121.0` with matching metadata and no inspect warnings.

*
  2026-04-15T12:33:07-07:00
  Requestor: operator
  Action: Add a real changelog, require its maintenance in repo guidance, keep
  `completed-tasks.txt` distinct from product change history, and draft a
  TheKnowledge ECR so changelog preservation can become SOP upstream.
  Status: completed by Codex on 2026-04-15T12:33:07-07:00. The repository
  now has `CHANGELOG.md`, a repo-local commit wrapper that preserves pending
  queue entries in the changelog before delegating to TheKnowledge's standard
  commit helper, updated AGENTS and workflow docs, and a draft upstream ECR
  under `ECRs/TheKnowledge/`.

*
  2026-04-14T17:13:43-07:00
  Requestor: operator
  Action: Proceed with the installer trust-boundary hardening so ambiguous
  isolated `HOME` sessions stop silently targeting the wrong user scope.
  Status: completed by Codex on 2026-04-14T17:13:43-07:00. The repository
  now has an approved proposal and specification for explicit user-home
  targeting, shared install-scope helpers, installer flags for
  `--user-home` and `--allow-isolated-home`, updated docs and help text, and
  tests plus smoke checks covering isolated-home rejection.

*
  2026-04-14T14:40:27-07:00
  Requestor: operator
  Action: Document the runtime-fidelity discussion fully and implement the
  approved startup-preflight and runtime-diagnostics recommendations.
  Status: completed by Codex on 2026-04-14T14:40:27-07:00. The repository
  now has an approved proposal, a compact specification, launcher preflight
  support, richer inspect and self-test diagnostics, updated docs, and test
  coverage for the new runtime-resolution behavior.

*
  2026-04-14T14:20:29-07:00
  Requestor: operator
  Action: Refine the runtime-fidelity draft into a reviewable proposal with
  recommended changes and an explicit development-versus-security assessment.
  Status: completed by Codex on 2026-04-14T14:20:29-07:00. The under-review
  proposal now leads with the best-practice assessment, recommends a concrete
  change sequence, and frames the launcher and sandbox tradeoffs in reviewable
  terms.

*
  2026-04-14T14:14:35-07:00
  Requestor: operator
  Action: Document the desired resulting-project runtime model, the current
  `codex-wrangler` situation, and recommended future work around runtime
  fidelity and sandbox alignment.
  Status: completed by Codex on 2026-04-14T14:14:35-07:00. The repository
  now has a README runtime-boundary clarification plus an under-review
  proposal that records the design tension, current launcher behavior, and
  phased follow-up recommendations.

*
  2026-04-01T10:20:15-07:00
  Requestor: operator
  Action: Investigate and correct the `install.sh` standard-install failure
  where the project install hook fell back to `/usr/bin/python3.13` and hit
  Debian's externally managed environment guard.
  Status: completed by Codex on 2026-04-01T10:20:15-07:00. The project
  install hook now preserves the managed venv interpreter path instead of
  resolving its symlink target, regression coverage models the symlinked
  venv layout explicitly, and the full local quality gate passed with `71`
  tests green.

*
  2026-04-01T01:42:16-07:00
  Requestor: operator
  Action: Correct the install-policy docs and implementation so the default
  `install.sh` path is user-local standard install, development mode is
  explicit, and system installs require `--system`.
  Status: completed by Codex on 2026-04-01T01:42:16-07:00. The repository now
  follows that corrected install-policy contract in the managed entrypoints,
  repo-specific install hook, and user-facing docs, and the full local
  quality gate passed with `69` tests green.

*

  2026-03-31T20:48:26-07:00
  Requestor: operator
  Action: Document the interpreter-environment-precedence follow-up proposal
  and start implementing the next slice needed to meet that standard.
  Status: completed by Codex on 2026-03-31T20:48:26-07:00. The repository now
  has the approved follow-up proposal, a repo-bound developer launcher hook,
  pyenv-runtime reuse in the stable user installer, updated docs, and a full
  passing local quality gate with `63` tests green.

*

  2026-03-31T20:08:27-07:00
  Requestor: operator
  Action: Adopt the converged managed bootstrap design in both this repository
  and TheKnowledge `trunk`, write the plan up as an approved proposal, and
  implement it.
  Status: completed by Codex on 2026-03-31T20:08:27-07:00. The parent repo now
  follows the managed `install.sh` plus `scripts/install-stage-2.py` contract,
  retains `scripts/install_user_tool.py` for stable CLI installs, and passed
  the full local quality gate plus `53` pytest cases under the refreshed
  pinned toolchain.
