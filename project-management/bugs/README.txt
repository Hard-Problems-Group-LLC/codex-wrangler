Bug Records
===========

Keep the ordered bug summary files at this directory root for prioritization
and lifecycle summaries.

Use the status subdirectories for detailed bug records, reproduction notes,
and supporting material:
- `open/`
- `in-progress/`
- `closed/`

Keep the detailed records aligned with the summary files when a bug changes
status.
