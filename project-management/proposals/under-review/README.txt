Under Review Proposals
======================

Use this directory for proposals still under active debate.

By default, proposals here are documentation-only and must not be
auto-implemented unless an operator explicitly authorizes movement into
`approved/`.
