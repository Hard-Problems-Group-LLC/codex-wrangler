# Open TheKnowledge ECRs

Use this directory for drafted TheKnowledge requests that are still local to
the consuming project and are not yet under active upstream handling.

When a request starts moving through a writable TheKnowledge workflow, move it
to `../in-progress/`.
