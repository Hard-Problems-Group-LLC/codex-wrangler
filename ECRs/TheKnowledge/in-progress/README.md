# In-Progress TheKnowledge ECRs

Use this directory for source ECRs that are actively being carried into a
writable TheKnowledge workflow.

When the upstream proposal, implementation, rejection, or deferral record is
written, move the source ECR to `../closed/` and add a note pointing to the
TheKnowledge record that resolved it.
